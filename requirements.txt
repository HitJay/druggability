# ── 学术检索 API 客户端 ────────────────────────────────────────────
pyalex>=0.14
semanticscholar>=0.8
arxiv>=2.0
biopython>=1.80
crossrefapi>=1.5
habanero>=2.0

# ── PDF 解析 ──────────────────────────────────────────────────────
pymupdf>=1.23
pdfplumber>=0.10

# ── 生医方向 ──────────────────────────────────────────────────────
chembl-webresource-client>=0.10
mygene>=3.2

# ── Druggability 评估 ────────────────────────────────────────────
# fpocket: 需单独编译安装（https://github.com/Discngine/fpocket）
# 可选依赖：torch（PUResNet 推理）、selenium（DoGSiteScorer 批量）
# pip install torch selenium
# ── 工具 & Notebook ──────────────────────────────────────────────
jupyterlab>=4.0
pandas>=2.0
tqdm>=4.60
python-dotenv>=1.0
rich>=13.0
requests>=2.28

# ── 可选：scispacy NER (需额外下载模型) ──────────────────────────
# pip install scispacy
# pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_sm-0.5.4.tar.gz

# ── 可选：paper-qa RAG (需 LLM API key) ─────────────────────────
# pip install paper-qa
